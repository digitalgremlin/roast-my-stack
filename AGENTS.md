# Agent instructions

- Follow `docs/plan.md` in task and tier order; use `docs/spec.md` for design context.
- Use TypeScript ESM, Apify SDK v3, tests first, and keep `tsc --noEmit` clean.
- Build detection rules here from public signals. Never import or vendor another detector or signature database.
- Keep Dr. Gordon Pelican PG-13 and focused on technology, never people.
- Preserve user changes and keep one focused commit per plan task.
